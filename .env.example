# Voice Narrator Environment Configuration

# Server Port (default: 3000)
PORT=3000

# Whisper model size for transcription: tiny, base, small, medium, large (default: small)
WHISPER_MODEL=small

# Optional: Gemini API Key for transcription storyboard prompt generation
GEMINI_API_KEY=your_gemini_api_key_here
